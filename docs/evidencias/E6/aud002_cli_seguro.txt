# AUD-002 — scripts/01 e 02 envolvidos em rag.cli_seguro() + guarda estática
# Auditoria Gemini 3.1 Pro, achado AUD-002 (Média). Ticket #24 (o AUD-002 duplicava o #24, já
# aberto; a issue #55 criada pela auditoria foi fechada como duplicata do #24). Data: 2026-09-18.
#
# Desvio consciente do #24: ele sugeria estender e6_ollama_desligado_scripts a 01/02. Em vez disso,
# guarda ESTÁTICA e6_cli_seguro — rodar 02 com o Ollama fora zeraria o índice (indexar() faz
# delete_collection() antes de embutir). A prova runtime de 02 abaixo usa backup→rodar→restaurar.

Contexto: scripts/03–07 e opcional/*.py já envolviam a execução em `with rag.cli_seguro():`;
só 01 e 02 ficavam de fora (confirmado por `grep cli_seguro`). Com o Ollama fora, os dois quebravam
com traceback cru em vez da mensagem clara + sys.exit(2) do context manager.

A checagem runtime existente, e6_ollama_desligado_scripts, usava o glob `0[3-7]_*.py` e nunca
exercia 01/02 — a lacuna exata do achado. A nova guarda e6_cli_seguro é ESTÁTICA (só lê o texto),
porque rodar 02 com o Ollama fora zeraria o índice: indexar() faz delete_collection() ANTES de
embutir (rag.py:188,199), então o embed falha depois da coleção já ter sido apagada.

== Guarda estática e6_cli_seguro — todos os scripts LLM protegidos (exit 0) ==
$ .venv/Scripts/python ferramentas/verificar.py e6_cli_seguro
AUD-002 scripts/00_checar_ambiente.py      usa_llm=False cli_seguro=False → ok
AUD-002 scripts/01_preparar_corpus.py      usa_llm=True  cli_seguro=True  → ok
AUD-002 scripts/02_indexar.py              usa_llm=True  cli_seguro=True  → ok
AUD-002 scripts/03_buscar.py               usa_llm=True  cli_seguro=True  → ok
AUD-002 scripts/04_dois_estagios.py        usa_llm=True  cli_seguro=True  → ok
AUD-002 scripts/05_shap.py                 usa_llm=True  cli_seguro=True  → ok
AUD-002 scripts/06_com_sem_contexto.py     usa_llm=True  cli_seguro=True  → ok
AUD-002 scripts/07_ollama.py               usa_llm=True  cli_seguro=True  → ok
AUD-002 opcional/avaliacao_estilo_ragas.py usa_llm=True  cli_seguro=True  → ok
AUD-002 opcional/calcular_shapley_chunks.py usa_llm=True  cli_seguro=True  → ok
[e6_cli_seguro em 0.3s]
# 00 corretamente usa_llm=False (só chama verificar_ollama e trata o Ollama fora por conta própria).

== Guarda REPROVA quando um script LLM perde o cli_seguro (prova negativa) ==
# Script-isca temporário `scripts/_zz_isca_guarda.py` com `rag.responder('x', [])` e sem cli_seguro:
AUD-002 scripts/_zz_isca_guarda.py         usa_llm=True  cli_seguro=False → FALTA
AUD-002 scripts que falam com o Ollama sem rag.cli_seguro(): ['scripts/_zz_isca_guarda.py']
EXITCODE=1
# Isca removida após o teste.

== Runtime: scripts/01 com o Ollama inalcançável (OLLAMA_HOST=http://localhost:11999) ==
# --regerar-resumos força a chamada ao LLM; 01 não escreve no ChromaDB, então é seguro rodar direto.
...
3) Resumos gerados pelo LLM a partir do abstract (no idioma do artigo)

ERRO: Não consegui falar com o Ollama em http://localhost:11999. Abra o aplicativo Ollama (ou rode
`ollama serve`) e tente de novo. Detalhe: Failed to connect to Ollama. ...
EXITCODE=2
# Mensagem clara, exit 2, SEM traceback.

== Runtime: scripts/02 com o Ollama inalcançável (backup → rodar → restaurar) ==
# chroma_db copiado para backup antes; indexar() apaga a coleção, então restaura-se ao final.
1) Chunking: 659 chunks em 7.2s
2) Embeddings + ChromaDB

ERRO: Não consegui falar com o Ollama em http://localhost:11999. ...
EXITCODE=2
# Mensagem clara, exit 2, SEM traceback.

== Índice restaurado e reverificado ==
$ .venv/Scripts/python ferramentas/verificar.py e2
2.2 total de chunks: 659
2.6 contagem neste processo novo (reabrindo chroma_db/): 659
[e2 em 3.0s]
# Coleção íntegra (659 vetores) depois do teste de 02.

Conclusão: 01 e 02 agora tratam o Ollama fora igual a 03–07 (mensagem clara + exit 2, sem traceback);
a guarda estática e6_cli_seguro impede recaída (novo script LLM sem cli_seguro reprova o build).
