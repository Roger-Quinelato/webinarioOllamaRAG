### DEVOLUÇÃO A1-02 → auditor A1

1. **Erro:** o achado descreve o fallback de `fontes_da_resposta` como se ele fosse uma falha
   descoberta agora, e o classifica como severidade média. Duas coisas estão erradas. Primeiro, o
   escopo: você testou só o caso em que **todas** as citações são inválidas, mas escreveu o achado como
   se a perda ocorresse sempre que há índice inválido — o caso misto, que é o provável na prática, já
   funciona. Segundo, o fallback é decisão registrada no próprio código, e o achado não a menciona nem
   argumenta contra ela.

2. **Onde:** `rag.py:359-365`:
   ```python
   # hazard: listar os k trechos recuperados como "fontes" mistura o que entrou no prompt com o
   # que a resposta de fato usou (achado 6.5) — aqui só entram os [n] que aparecem no texto
   # gerado; se o modelo não citou nenhum, cai para os recuperados, para nunca ficar sem fontes;
   ```
   "Se o modelo não citou nenhum, cai para os recuperados" é exatamente o comportamento que o achado
   reporta como surpresa. Índice fora de `1..n` é descartado por `indices_citados` (`rag.py:341`), o que
   torna o caso "citou só inválidos" indistinguível de "não citou nada" — por construção.

3. **Evidência contrária:**
   ```
   == CTO/A1-02 citacao mista valida+invalida vs so invalida ==
     indices_citados=[2] fontes=[2] <- 'Segundo [2] e [9].'
     indices_citados=[] fontes=[1, 2, 3, 4] <- 'Segundo [9].'
   ```
   Com uma citação válida presente, o índice inválido é simplesmente ignorado e só a fonte citada
   aparece. O fallback só entra quando não sobra nenhuma citação válida.

4. **Como refazer:** reescreva o achado com o escopo estreito que a evidência sustenta — "quando a
   resposta cita **exclusivamente** índices fora do intervalo" — e trate-o pelo que ele é: uma escolha
   de projeto documentada que pode ser inadequada num caso específico, não um defeito de
   implementação. Para sustentar a mudança, mostre que o caso acontece de verdade: procure em
   `resultados/com_sem_contexto.json` e em `docs/evidencias/E6/` alguma resposta real do `qwen2.5:1.5b`
   ou do `qwen2.5:3b` que cite apenas índice inexistente. Se não houver nenhuma ocorrência real, diga
   isso no achado e baixe a confiança — um caso hipotético não justifica mudar regra que já tem
   evidência de funcionamento (T01, `docs/evidencias/E6/e6_fontes.txt`).

5. **Critério de aceite:** o achado revisado precisa (a) limitar-se ao caso "só índices inválidos",
   (b) citar `rag.py:359-365` e dizer por que a decisão documentada ali não cobre esse caso,
   (c) trazer uma ocorrência real nas evidências existentes ou declarar explicitamente que não
   encontrou nenhuma, e (d) ficar em severidade baixa enquanto não houver ocorrência real.
