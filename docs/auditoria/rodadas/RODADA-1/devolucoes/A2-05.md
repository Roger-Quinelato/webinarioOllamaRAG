### DEVOLUÇÃO A2-05 → auditor A2

1. **Erro:** o achado está factualmente certo, mas invade o escopo de outro eixo e duplica trabalho. Ele
   enumera célula por célula os números defasados de `docs/medicoes.md` — arquivo que pertence ao eixo
   A5, que auditou o mesmo ponto em maior profundidade e o quebrou em sete achados separados (A5-01 a
   A5-07), cada um com a evidência específica que o contradiz. Mantido como está, o consolidado geraria
   dois tickets para o mesmo conserto.

2. **Onde:** `docs/VERIFICACAO.md:230` (critério 9.1 ✅) é seu; `docs/medicoes.md:20,31,32,53` não é.
   A tabela §3 do protocolo dá `docs/medicoes.md` ao eixo A5 e `docs/VERIFICACAO.md` +
   `docs/evidencias/**` ao seu.

3. **Evidência contrária:** nenhuma — o fato é verdadeiro e foi confirmado. O que existe é sobreposição:
   `A5-achados.md` cobre os mesmos números com granularidade maior, incluindo um caso que você não
   pegou (A5-07: `medicoes.md:53` publica tempos do `qwen2.5:3b` citando um `E8/apptest.txt` que hoje
   registra `qwen2.5:1.5b` e 44 s, e a linha 100 usa esses tempos para orçar o bloco 7).

4. **Como refazer:** reescreva A2-05 restrito ao que é do seu eixo — **o critério 9.1 está ✅ e a
   evidência que ele cita não sustenta mais os números**. Esse é o achado de A2: um ✅ apoiado em
   evidência divergente. Não repita a lista de células; referencie A5-01 a A5-07 no campo `relacionado`
   e deixe a enumeração com o dono. Aproveite para cobrir o que só o seu eixo enxerga: existe alguma
   checagem em `ferramentas/verificar.py` que deveria ter detectado essa divergência e não detectou?
   (Você já mostrou em A2-06 que `e9_numeros` confere contra um arquivo append-only; conecte os dois.)

5. **Critério de aceite:** o achado revisado precisa (a) citar apenas `docs/VERIFICACAO.md` e
   `docs/evidencias/**` no campo `onde`, (b) afirmar o problema como "✅ sem evidência que o sustente",
   não como lista de números errados, (c) listar A5-01 a A5-07 em `relacionado`, e (d) dizer
   explicitamente qual checagem automatizada falhou em detectar, ou que nenhuma cobre esse caso.
